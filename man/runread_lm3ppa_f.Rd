% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/runread_lm3ppa_f.R, R/runread_sofun_f.R
\name{runread_lm3ppa_f}
\alias{runread_lm3ppa_f}
\title{Run LM3-PPA}
\usage{
runread_lm3ppa_f(drivers, makecheck = TRUE, parallel = FALSE, ncores = 2)

runread_lm3ppa_f(drivers, makecheck = TRUE, parallel = FALSE, ncores = 2)
}
\arguments{
\item{drivers}{A nested data frame with one row for each site and columns
named according to the  arguments of function `runread_pmodel_f_bysite()`}

\item{makecheck}{A logical specifying whether checks are performed to verify
forcings.}

\item{parallel}{A logical specifying whether simulations are to be 
parallelised (sending data from a certain number of sites to each core). 
Defaults to \code{FALSE}.}

\item{ncores}{An integer specifying the number of cores used for parallel 
computing. Defaults to 2.}
}
\value{
A tibble with one row for each site and outputs stored 
in the nested column \code{data}

A tibble with one row for each site and outputs stored 
in the nested column \code{data}
}
\description{
Runs the LM3-PPA model and loads output in once.

Runs the LM3-PPA model and loads output in once.
}
\examples{
\dontrun{
 mod <- runread_pmodel_f( df_drivers,
  params_modl, makecheck = TRUE, parallel = FALSE, ncores = 2 )
}
\dontrun{
 mod <- runread_pmodel_f( df_drivers,
  params_modl, makecheck = TRUE, parallel = FALSE, ncores = 2 )
}
}
